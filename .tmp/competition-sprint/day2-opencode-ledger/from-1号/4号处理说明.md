# 1号 Day2 报告 · 4号处理说明

来源：from-1号/day2-工作报告.md
整理：2026-08-13（4号）

## 1号 完成情况（✅ 属实，与仓库代码核对一致）

1. **主流程关键节点统一记录：完成** —— 诊断/画像/路径/讲义/代码实验/测评均记录状态，历史不覆盖。对应仓库 `7362530`（append-only worker ledger，已合并进 main）。
2. **记录阶段/时间/状态/产物：基本完成** —— 部分产物路径缺"文件存在验证"（1号 自述，遗留项）。
3. **与 2号 统一 ledger 字段：完成** —— entry_id/unit_name/execution_type/stage/started_at/finished_at/errors/manual_intervention/observability；worker_ledger 保留给前端。
4. **跑一次带 ledger 的完整流程：核心完成** —— 19 events + 11 ledger 记录，进入第2轮测评等待。

## 1号 报告的问题 · 4号核查结果

### 问题1：前端"下一题"按钮点击无效 ⚠️ 已处理
- 1号 描述：第1题答案选中后按钮显示可用，但点击不进入第2题；后续改用 API 继续，非 100% 纯前端闭环。
- **4号 调查结论（2026-08-13）**：
  - 按钮逻辑本身正常 —— 用 jsdom 交互测试复现（`src/role-d-ui-v2/src/diagnosis-next-button.repro.test.tsx`），选中答案→点下一题→成功进入第2题，34 pass / 0 fail。
  - **根因是 CSS 误导**：`.primary-action:hover` 没有 `:not(:disabled)` 保护，disabled 按钮 hover 时依然变亮上浮，看起来"可用"，实际点不动。
  - **已修复**：styles.css 加 `.primary-action:hover:not(:disabled)` 和 `:disabled` 禁用样式（变灰、无 hover、cursor:not-allowed）；secondary-action 同样处理。
  - 提醒 1号：**Ctrl+F5 强刷清缓存**后再试。
- 遗留：1号 那次 session 是用 API 续跑的，前端闭环未 100% 验证；修复后建议重跑一次纯前端闭环。

### 问题2：Role C 代码实验可信校验失败 ⚠️ 偶发，非阻塞
- 第一次生成没过门禁，重新运行后 concept-tutor/code-lab/tiered-evaluator 全部成功。
- **4号 结论**：这是模型输出波动（参考实现 vs 隐藏测试不匹配），重试即过；之前 Day1 已确认 C 门禁本身能过（L1 闭环 10/10 advance）。属已知偶发，不是系统缺陷。

## 给队友的注意

- 1号 报告中"部分产物路径没有严格文件存在验证"是遗留项，建议 Day3/4 补上（3号 的 artifact-map 已部分覆盖）。
- 前端修复在本地分支（未提交 PR），等 4号 Day2 产物一起提交。
