# 2号 Day2 报告 · 4号处理说明

来源：from-2号/day2-工作报告.md
整理：2026-08-13（4号）

## 2号 完成情况（✅ 属实，已与仓库核对）

1. **调度证据导出代码完成** —— `src/orchestration/day2-opencode-evidence.ts`（156行）+ `scripts/opencode-orchestrator-demo.ts`（37行）+ 测试41行。
2. **本机真实会话验证** —— 18 条追加式调度历史，8 个执行单元 completed，会话停在等待测评；产物映射导出 10 个文件带完整性校验。
3. **专项测试** —— 8 pass / 0 fail，typecheck 通过（专项验证，非全量）。
4. **敏感扫描** —— 无 API Key / Authorization / 模型配置进入 PR。

## ⚠️ 需要指出的差异：PR#6 已合并，报告说"尚未合并"

2号 报告写"PR #6 已开放，尚未合并"，但 4号 拉取最新仓库时确认：

```text
main HEAD = 9d4491c Merge pull request #6 from jiangdongouyang-lab/role2/day2-opencode-ledger
97d0e31 feat(orchestration): export truthful day2 agent evidence（2号 提交）
```

**PR#6 已合并进 main**（报告可能写于合并前）。验收时以仓库 main 为准，代码都在。

## 重要边界（写报告/演示时必守）

1. **只能称"OpenCode 风格"结构化调度证据**，不能宣传成"真实 OpenCode 子 Agent 调用"——本次未观测到 OpenCode task ID（2号 自述，诚实红线）。
2. **worker_ledger = 最新状态，worker_ledger_history = 不可覆盖历史**——引用时别搞混。
3. **concept-tutor/code-lab 早期 completed 无 output_refs**——历史缺口保留未反填，不能反推。
4. **本地 .tmp 运行证据含内部会话引用，不进公开 PR**（与 4号 的脱敏处理一致）。

## 遗留缺口（4号 相关）

| 缺口 | 说明 | 归属 |
|---|---|---|
| 前端创建计划前未强制 Docker ready | 仓库层面预检缺口，PR#6 未处理 | **4号 前端门禁**（Day3/4 待办） |
| 产物路径文件存在验证 | 1号/2号 均提到 | 3号 artifact-map 部分覆盖 |
| 演示机环境准备 | 模型配置/Docker/runner 镜像需提前备 | 全员（演示前检查） |

## 对 judge-checklist 的影响

2号 的"OpenCode 风格≠真实调用"边界 → judge-checklist **B5 项**（不是顺序调API）验收时按"结构化调度证据"标准打分，不按"真实 OpenCode 集成"标准。
